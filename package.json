{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.8.4"
  },
  "scripts": {
	"dev": "concurrently \"cd src/server && yarn start\" \"cd src/client && yarn dev\"" 
  },
  "workspaces": [
    "packages/*",
	"src/*"
  ]
}
