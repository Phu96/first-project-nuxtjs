{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.8.4"
  },
  "scripts": {
	"dev:client": "cd src/client && yarn dev", 
	"start:server": "cd src/server && yarn start",
	"dev:client": "cd src/client && yarn dev",
	"start:client": "cd src/client && yarn start",
	"build:client": "cd src/client && yarn build",
	"generate:client": "cd src/client && yarn generate",
	"lint:client": "cd src/client && yarn lint",
	"precommit:client": "cd src/client && yarn precommit"
	
  },
  "workspaces": [
    "packages/*",
	"src/*"
  ]
}
